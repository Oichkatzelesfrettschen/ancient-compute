[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = .
addopts =
    --verbose
    --strict-markers
    --tb=short
    -W error
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests
    physics: Physics and simulation tests
